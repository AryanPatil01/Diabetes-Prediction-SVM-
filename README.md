# Diabetes-Prediction-SVM-
Support Vector Machine (SVM) based classification model for diabetes prediction using the Pima Indians dataset. Includes data preprocessing, feature scaling, class imbalance handling, model evaluation, and an interactive inference interface.
